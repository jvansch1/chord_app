## Next Steps
Figure out how to more programtically handle chord, it would be very tedious to build out each one especially as we add high extensions.

## Running tests
`uv run pytest`

## Run FastAPI server
`uv run fastapi dev src/api/chord.py`